# 筛选功能测试指南

## 🔄 第一步：硬刷新页面

**Mac**: `Cmd + Shift + R`
**Windows/Linux**: `Ctrl + Shift + F5`

## 🧪 第二步：执行测试

1. 打开项目列表页：http://localhost:5173/projects
2. 登录（admin / admin）
3. 打开开发者工具（F12）→ Console

## 📋 第三步：查看日志

点击"筛选"按钮后，应该看到以下日志序列：

```
🔍 [ProjectList] 执行筛选，参数: {...}
🔧 Store setFilters 被调用，filters: {...}
📞 Store 立即调用 fetchProjects
📡 fetchProjects 被调用，当前 filters: {...}
📤 API 请求参数: {...}
📊 获取项目列表 - 参数: {...}
🔍 筛选条件: {...}
✅ 获取到 X 个项目
🔄 准备更新 store 状态: {...}
✅ store 状态已更新，当前 projects 数量: X
🔔 [ProjectList] tableData 重新计算，数量: X  ← 新增
🔔 [ProjectList] projects 状态变化，当前数量: X
✅ [ProjectList] setFilters 完成
```

## ✅ 验证成功标志

- [ ] 控制台显示完整日志链
- [ ] 看到 "tableData 重新计算" 日志
- [ ] **表格数据实时更新**

## ❌ 如果还是失败

请截图控制台的完整日志并发送给开发者。

## 🔧 紧急调试代码

如果上述都正常但表格还是不更新，在控制台执行：

```javascript
// 强制刷新测试
(() => {
  const table = document.querySelector('table');
  console.log('Table Element:', table);
  console.log('Table Rows:', table?.querySelectorAll('tbody tr').length);

  // 触发 React 重新渲染
  window.dispatchEvent(new Event('resize'));
})();
```
